apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wil-argo
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/osmangokturk/manifestForIncOTProject.git
    targetRevision: HEAD
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      enabled: true

